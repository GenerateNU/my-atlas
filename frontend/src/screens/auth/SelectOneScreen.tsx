import { SafeAreaView, Text } from 'react-native';

const SelectOneScreen = () => {
  return (
    <SafeAreaView>
      <Text>Select One SCREEN</Text>
    </SafeAreaView>
  );
};

export default SelectOneScreen;
